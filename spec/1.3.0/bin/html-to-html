#!/usr/bin/env bash

version=1.3.0.5

source "${ROOT:-$PWD}/.bpan/run-or-docker.bash"

check() (
  need python3 3.8
  need python bs4 roman yaml
)

dockerfile() (
  from alpine
  pip beautifulsoup4 roman pyyaml
)

run "$@"
