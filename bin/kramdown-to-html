#!/usr/bin/env bash

version=1.3.0.1

source "${ROOT:-$PWD}/.bpan/run-or-docker.bash"

check() (
  need ruby 2.3
  need ruby kramdown
  need ruby kramdown-parser-gfm
)

dockerfile() (
  from alpine
  gem kramdown
  gem kramdown-parser-gfm
)

run "$@"
